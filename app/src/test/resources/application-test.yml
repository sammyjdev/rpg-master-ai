spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/rpgmaster_test
    username: rpgmaster
    password: rpgmaster

  ai:
    ollama:
      base-url: http://localhost:${OLLAMA_MOCK_PORT:11434}
      embedding:
        model: bge-m3
      chat:
        model: llama3.2:3b

qdrant:
  host: ${QDRANT_HOST:localhost}
  port: ${QDRANT_GRPC_PORT:6334}

logging:
  level:
    com.rpgmaster: DEBUG
